require 'packaging'
Pkg::Util::RakeUtils.load_packaging_tasks

namespace :package do
  desc "Bootstrap isn't needed since we're using packaging as a gem"
  task :bootstrap do
    puts "this method isn't needed since we're using packaging as a gem"
  end
  desc "Implode isn't needed since we're using packaging as a gem"
  task :implode do
    puts "this method isn't needed since we're using packaging as a gem"
  end
end
